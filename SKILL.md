---
name: photoshop-psd-production
description: Inspect, edit, automate, validate, export, commit, and organize Photoshop PSD/PSB work. Use for copy replacement, embedded image placement, clipping masks, scene cards, artboards, catalogs, Dingdong 好食光 detail pages and main images, large documents, previews, JPG/PDF export, used-asset organization, or reusable template registration.
---

# Photoshop PSD Production

## 核心原则

1. 把用户明确提出的制作范围视为本次正常编辑授权。不要为工作副本、提交或已要求的导出重复索要批准。
2. 只在请求未明确包含删除文件、覆盖既有交付物、母版改名/登记或外部发布时询问。
3. 始终使用工作副本。默认禁止创建或保留目标 PSD 备份；只有用户在当前任务中明确要求备份时才可启用。提交后重开并验证目标文字和图片。
4. 对文字逐字符写入并保持字号。溢出或拥挤记为警告，必须查看预览，不要自动缩字或改文案；叮咚逐字步骤即使被遮挡也直接写入并完成提交，随后报告实际被遮挡的原文。
5. 图片使用唯一图层选择器，Place Embedded，验证父组、顺序、剪贴和覆盖。
6. 统一使用 PowerShell 7。所有文本显式使用 UTF-8 无 BOM，不依赖系统默认编码或 profile。
7. 所有两段式标题的上下段不得使用相同的开头词语或句式片段；标题与下方说明属于同一两段式组合时也适用。同一文字层中的两行与分在不同文字层中的两段都要比较，不限于版4或某个产品。初稿、修改稿和套版前均须检查，发现重复先改写对应候选，再展示审核。

## 两步生产

先阅读 [job-workflow.md](references/job-workflow.md)。

```powershell
.\scripts\prepare-psd-job.ps1 -TaskPath C:\work\job.json -RunPath C:\work\run.json
```

查看生成的预览。硬错误停止；警告在最终汇报中说明。视觉检查通过后运行：

```powershell
.\scripts\complete-psd-job.ps1 -RunPath C:\work\run.json
```

用户说“直接完成”时连续执行两步，中间自行查看预览；用户只要求预览时停在 `prepared`。

普通任务只保留任务 JSON、一个 `run.json`、工作副本、预览和用户要求的交付文件。不要创建 approval、preflight、validation 或 export-plan 文件。叮咚详情页文案在任务生成前允许保留一个由标准脚本生成的 copy review，用于展示语法、字数和确认哈希。

叮咚聊天九列审核必须每个板块只显示一行：同板块的标题、上下说明和其他文字按 PSD 阅读顺序合并进同一行的原文/新文单元格，禁止把版4主料标题与口感说明、版6标题与两条说明等拆成独立审核行。标题与下方说明还必须分别承担不同信息，禁止用同义词重复描述同一食材外观、口感、风味或卖点。用户说“确认文案”“文案确认”“按这个套版”等同义表达时，把它视为对当前已展示文案的一次性套版授权。立即冻结该版本，内部生成并校验 `copy.json` 和 SHA-256；内容完全一致且校验通过后，直接生成详情页任务、创建工作副本、写入文字并检查预览。SHA-256 只用于防止确认后被改动，不是第二次用户审批。不要再询问“确认哈希”“确认生成任务”或“确认写入 PSD”。只有校验失败、正式文件与已确认九列表不一致、目标 PSD 不明确或用户明确只要审核稿时才停下。

## 任务选择

- 常用模板：在任务中填写 `source.templateId`。
- 一次性 PSD：填写 `sourcePsdPath`。模板登记不是生产许可。
- 普通改字、放图、场景卡：直接填写 `textReplacements`、`imageTransfers` 或 `sceneCards`。
- 叮咚详情页和主图：阅读 [dingdong.md](references/dingdong.md)，使用对应任务生成器。详情页拟写或替换文案时还必须完整阅读 [dingdong-copy-grammar.md](references/dingdong-copy-grammar.md)。先快速盘点目录并一次性读取相关 Word、表格和文本事实；初稿阶段不要渲染只用于取文本的 Word、不要逐张检查全部图片、不要创建正式 `copy.json`。先在聊天中给出九列并列审核，解决术语冲突和用户修改后再生成正式审核文件。主图阶段复用已确认文案。正式详情页已经放图时，把它作为七图主图的默认且唯一图片来源，按已登记版块关系一次性自动套图；只有正式详情页缺图、来源不唯一或用户明确要求改用其他原图时，才进入逐项选图确认。禁止为了取图制作临时详情页。主图提交并重开验证后，立即在同一次完成流程中归类正式详情页全部可见智能对象实际引用的根目录图片；严禁用七项主图映射的 `sourceLayerIds` 过滤详情页素材，不再询问或留到后续任务。
- 叮咚文案确认阶段收到修改要求时，把需要重新生成的行重新视为未确认候选，并从模板原文与当前产品事实出发，完整重走初稿的语法、字数、分句、版式、事实依据、逐字来源、术语、版块专属禁写和固定文字规则；不得基于旧候选自由润色、降低标准或事后补标签。只重生成用户点名行及确有事实或术语依赖的联动行，其他行逐字符不变，并重新展示完整九列表。若修改发生在一版文案已确认之后，旧冻结版本、`copy.json`、SHA-256、任务和套版授权立即失效；必须先展示并重新确认符合相同初始规则的新九列表，禁止复用旧确认继续写入 PSD。
- 用户说“导出某路径的图片”或语义等价表达时，阅读 [dingdong.md](references/dingdong.md) 的整包导出规则并运行 `export-dingdong-image-package.ps1 -ProductDirectory <指定路径>`。只扫描指定路径根层的当前商品 PSD：按文档结构唯一识别详情页和主图，不按文件名猜测；详情页固定同时导出完整长图和切片，切片只能读取该商品详情 PSD 自己保存的水平参考线和当前画布首尾，永久禁止读取母版、模板定义、模板索引、历史切片高度或母版参考线作为切片依据。整包导出主图必须按名称顺序导出画板 0–7，不排除画板 0。商品根层如有唯一的 `白底图` 或 `标签` JPG/JPEG/PNG/WEBP，或精确文件名 `包装图.jpg`、`白标.jpg`，原字节复制进 `主图/` 后随主图副本一起压缩；同类多文件或不支持格式立即停止。商品目录内固定保留一套时间戳命名的未压缩 `详情页/`、`主图/`、`已使用/`，并验证它们在压缩阶段未变化；只把临时副本交给固定 `image-compressor.exe` 智能压缩并在 `D:\谷鱼视觉\小梅园` 生成 7z。指定路径 `已使用` 中的图片禁止压缩并保持原始字节和 SHA-256 不变。
- 产品目录、大型 PSB、多画板导出和素材整理：阅读 [advanced-workflows.md](references/advanced-workflows.md)。

## 叮咚时限

把“详情页文案替换、详情页生成、七图主图生成和最终验证”的主动处理时间控制在 30 分钟内。用户等待确认、Photoshop 无响应或异常的大文件 I/O 不计入主动处理时间，但必须立即说明。不得用重复扫描、重复渲染、全量图片盘点、自建一次性分析脚本或反复重写 JSON 消耗时间；同一资料、索引、审核稿和已验证 PSD 在一次任务中只读取或生成一次。

确认文案后使用快速通道：详情页直接套版。用户要求制作主图时，除恢复已经 `prepared` 或 `committed` 的现行主图运行外，第一项生产动作必须运行 `invoke-dingdong-product.ps1 -PrepareMain:$false`，针对当前已保存的正式详情 PSD 刷新索引并解析七项图片来源；该预检返回前禁止读取产品原图选图、制作选图总览、手工编写 mapping 或进入直接图片模式。`imageChangedCount` 只表示所属任务本次执行的图片替换数量，永久禁止据此判断正式详情 PSD 当前是否含图。七项来源唯一时复用同一入口继续准备和提交主图；来源缺失或不唯一时在打开主图母版前停止并报告具体版块。任务中断后读取 `dingdong-product-state.json` 续跑；尚未准备主图时只复用详情 PSD、详情索引和文案三项哈希都命中的 mapping 或 job，已经 `prepared` 或 `committed` 的运行则保持原运行并交给完成校验检查来源稳定性。

## 阻断与警告

阻断 PSD/素材缺失、未保存来源、目标图层不唯一、文字写入不一致、字号变化、图片剪贴/覆盖错误、保存/提交/重开失败，以及未授权的输出覆盖。凡模板通过 `protectedTextTargets`、`protectedPathPrefixes`、`unchangedTextTargets` 或 `sync-detail-current` 声明为不可修改的文字，均永久只读：文案校验、任务生成和工作副本创建前任一层检测到改动都立即报错。唯一窄例外是叮咚主图画板3的步骤数量标题：当模板标题中的步骤数字与详情定义里唯一、从 1 连续的现行步骤数不一致时，只允许把标题中唯一的 `N步` 数字同步为已登记的 1、2、3 或 4；标题其余字符、字号和版式仍不可修改，数字已经一致时保持模板原文，未登记步骤数、非连续步骤或任何扩大改写均以 `invalid-step-count-sync` 报错。不得询问解锁，不得接受用户授权、`protectedOverrides` 或手写任务绕过其他只读位置。叮咚 `structure-adapted` 普通改写还必须严格拒绝完整二字“入+名词”词组：仅当 `proposedSegments[].text` 恰好为“入”加一个中文字符时，以 `forbidden-ru-noun-pattern` 报错；“加入香菜”“放入蔬菜”以及被空格拆开的 `入 味` 不命中。用户逐字来源 `verbatim` 不适用此禁写规则。用户明确要求保留完整短语，或针对当前九列表指定“把 A 改为 B”“就按这个字样”等精确替换时，也视为用户逐字来源：把合并后的目标全文登记为 `verbatim`，令 `sourceText` 与最终文字逐字符一致，并记录用户原话 `userInstruction`；此时允许“入味”等用户指定字样。该例外不得用于代理自行生成的普通候选，也不得伪造或扩大用户指令。叮咚详情页版4的普通适配文案永久禁止描述分装、装好、切好、整包及同类预制或包装状态；脚本同时拦截配好、备好、预切、切配、预处理、预制、预装、按包、单包、包装、袋装、盒装等表达，并以 `forbidden-block4-prep-packaging-copy` 报错。版4也永久禁止复述食用步骤、火候、时长或烹饪操作；命中锅中、大火、中火、小火、下入、放入、加入、倒出、备用、翻炒、炒制、断生、变色、分钟等步骤词时以 `forbidden-block4-instruction-copy` 报错，未命中关键词但语义仍是步骤时也必须在聊天审核中拒绝。版6的两个说明目标永久各自描写一种配菜；每项必须引用一个 `category: "side-dish"` 且带 `terms` 的事实、正文命中其配菜词，并且两个目标不得绑定同一种配菜，否则阻断。叮咚详情页还要在创建工作副本前阻断未通过的 `copyVersion: 2` 文案和确认后哈希变化。

叮咚详情页支持已登记的 1 步、2 步、3 步和 4 步母版；必须按当前商品的连续步骤数选择对应定义，禁止把不同步骤数的文案套入其他定义。版6主标题永久要求正文前两个字符为“特调”。模板定义使用 `requiredPrefix: "特调"` 登记，正式文案校验和 Photoshop 前置校验均以 `required-copy-prefix` 阻断其他开头。版8场景文案永久逐字符固定为两行“下班回家轻松煮”“煮好后热乎上桌”，使用 Photoshop CR 换行并保留末尾 CR；模板定义通过 `requiredText` 登记，两个校验层均以 `required-exact-copy` 阻断任何其他文字、空格或换行形式。

把模板缓存变化、低分辨率、边缘裁切、文字溢出和非关键装饰变化作为警告。警告不需要审批文件，但必须检查预览并向用户说明。叮咚逐字步骤遮挡不阻断提交，也不触发改文案或缩字请求；完成后逐个步骤报告“全部可见”，或准确写明可见终点和被遮挡全文。

## 完成标准

只有 `run.json` 的 `psdState` 为 `committed`、目标 PSD 哈希等于已验证工作副本、重开验证通过，才报告 PSD 完成。只有 `workflowState` 为 `complete` 才报告整条流程完成；`needs-attention` 表示 PSD 已提交但归类或导出仍需处理。叮咚主图还必须已经在同一完成调用中执行详情页已使用图片归类，并把结果写入同一运行记录；归类失败不回滚已完成 PSD。最终导出失败同样不回滚已完成 PSD，并要明确报告。
